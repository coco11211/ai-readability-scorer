# Electron download settings
# Use this if default download source is blocked
# electron_mirror=https://npmmirror.com/mirrors/electron/

# Build settings
build_from_source=false

# Cache settings
cache=/root/.npm
